# Backend used only by Vite's local same-origin /api proxy.
VITE_API_PROXY_TARGET=http://localhost:8080

# Production intentionally keeps API requests on /api; vercel.json proxies them.
# Avoid VITE_API_URL for browser sessions because cross-origin cookies and relative
# product-image URLs rely on the same-origin proxy architecture.
