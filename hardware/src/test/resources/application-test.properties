# Tests opt into concrete values only where a scenario needs them. Empty defaults
# keep unrelated test contexts free of packaged credentials and contact data.
app.admin.email=
app.admin.password-hash=
app.store.whatsapp-number=
